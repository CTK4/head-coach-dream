# Legacy/archived tooling

## Python calibration harness archive

The historical top-level `main.py` calibration harness has been moved to `tools/legacy/main.py` and is now treated as archived tooling.

- Owner in commit history: CTK4 (`7d5cbe5`).
- Purpose: offline simulation realism calibration experiments.
- Invocation/constraints: see `tools/legacy/README.md`.
